// Relations are handled via foreign key constraints in MySQL
// The Drizzle ORM relations API is available for complex queries if needed
// For now, we use the schema directly with join queries
export {};
